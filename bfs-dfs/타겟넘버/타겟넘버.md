## DFS 문제

- 경로 개수 / 조합 수 등
- 더하거나 빼는 것 2가지의 조합을 갖고 모든 가지를 순회하며 조건에 맞는 개수만 찾아내기

DFS - 스택 활용

1. (연산한 개수, sum 값)을 튜플로 스택에 넣는다
2. 스택에 해당 값의 다음값을 "더한 결과"와 "뺀 결과"를 차례대로 넣는다.
3. 연산한 개수가 주어진 리스트 길이와 같아지면 연산 결과를 비교한다

- 연산 결과가 target 값과 같으면 +1
- 아닐 경우 다음 연산 시작

스택을 모두 돌면 모든 조합에 대해 순회가 끝난다
